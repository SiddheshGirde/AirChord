# AirChord dependencies

# Phase 1
opencv-python>=4.9
numpy>=1.26

# Phase 2 - hand detection
# NOTE: mediapipe has, on some releases, required numpy<2. If
# `pip install -r requirements.txt` reports a numpy version conflict, run:
#   pip install "numpy<2"
# and then re-run this install.
mediapipe>=1.0

# Phase 4 - audio playback (chords are synthesized, so no audio assets needed)
pygame>=2.5
